# Amplitude ingestion key — public by design (it's baked into the client
# bundle for every visitor, same as any website's analytics snippet), safe
# to commit. See .env.example for the OpenAI key, which is the opposite.
VITE_AMPLITUDE_API_KEY=ae3ec993961e7870d5bfe22793d2abd1
