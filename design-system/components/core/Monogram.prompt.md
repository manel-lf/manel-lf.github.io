The brand mark — one letter, 800 weight, in a 32px near-black square with an 8px radius. There is no logotype; this is it.

```jsx
<Monogram letter="M" href="#/" />
```

Lifts 1px on hover. The same shape is the site favicon (`assets/logo.svg`). Do not scale it past ~40px or place it on a dark panel without inverting the square.
