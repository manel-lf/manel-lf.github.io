Circular 44px portrait for a testimonial. `alt` is deliberately empty — the name sits next to it.

```jsx
<Avatar src="assets/img/testimonial-emmi-kuusikko.jpg" name="Emmi Kuusikko" />
```

A missing or broken file falls back to two initials in muted mono-ish type, never a broken-image icon. Square crops only.
