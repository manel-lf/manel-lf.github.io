The prev/next pair that closes every case study and journal post.

```jsx
<nav className="caseNav">
  <CaseNavBtn direction="prev" label="Previous" name="Radisson Hotels" href="#/work/radisson" />
  <CaseNavBtn direction="next" label="Next" name="Jesterday" href="#/work/jesterday" />
</nav>
```

Always both, always wrapping the list, so no case study is a dead end. The next button mirrors right.
