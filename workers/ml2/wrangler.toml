name = "ml2-chat"
main = "index.js"
compatibility_date = "2026-09-01"

# The OpenAI key is NOT here. It is a secret:
#   npx wrangler secret put OPENAI_API_KEY
# Set a monthly spend limit on that key in the OpenAI dashboard too — that is
# the real cap if the endpoint is ever abused.
